const visibilityTypes = {
  PUBLIC: 'public',
  PRIVATE: 'private',
};

const stateTypes = {
  DEVELOPMENT: 'development',
  RELEASED: 'released',
};

module.exports = { visibilityTypes, stateTypes };
